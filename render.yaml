services:
  - type: web
    name: student-performance-ai-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /
    envVars:
      - key: OLLAMA_URL
        sync: false
      - key: OLLAMA_MODEL
        value: llama3.2
      - key: OLLAMA_TIMEOUT_SECONDS
        value: "120"
      - key: OLLAMA_API_KEY
        sync: false
